# spec-write leaves drafted specs uncommitted — Journal

## 2026-05-17 — Intake: /spec-write produced Phase B & C feature specs without committing; both gaps surfaced by /spec-execute orient

**Captured by:** waseric; persona-frame: intake
**Signal source:** text (operator-noticed pattern across two consecutive `/spec-execute` orient phases)
**New status:** `intake`
**Notes:**
- Captured during the Phase C `/spec-execute` orient phase on 2026-05-17. The orient report flagged `specs/20260517-finding-triage-skill/` as untracked and recommended an out-of-band commit before opening T-01. This is the second occurrence of the same pattern; Phase B exhibited it on 2026-05-16 and was remediated at commit `9a6d374`. Phase C's remediation commit is `c29f23b`. Operator directed an intake capture and explicit auto-commit of this finding before moving on to T-01 of the finding-triage-skill spec.
- Operator-directed deviation from finding-intake SKILL.md OP #7 ("Working-tree-leave, not auto-commit"): for this invocation only, the operator explicitly authorized auto-staging and auto-committing the produced finding as a one-step capture, because the captured pattern is *exactly* "an authoring step that does not commit." Leaving this finding untracked would have reproduced the meta-pattern under report. Surfaced here for RC-3b reviewers and the eventual triage of this finding — the deviation is operator-scoped to this invocation; OP #7 is unchanged as the skill's default.
- Pattern evidence is accessible via `git log --oneline -- specs/20260517-finding-intake-skill/` and `git log --oneline -- specs/20260517-finding-triage-skill/` — both directories' first commits are the `/spec-execute`-driven remediation commits, not `/spec-write` closeouts.
- Captured in structured-input mode by Claude on the operator's behalf; no interactive prompts answered. The operator's directive was "no input required from me." All fields derived from `/spec-execute` orient context (TITLE, SUMMARY) or session defaults (DATE = 2026-05-17 from environment; CAPTURED_BY = waseric per git config user.name; DOMAIN = methodology per signal substance; SHORT_NAME = `spec-write-leaves-specs-uncommitted`, derived per SKILL.md Phase 2 short-name rules — 5 word-pieces, 35 chars, no stop-word collisions).
- Minor methodology observation surfaced during execution but not load-bearing for this finding: the finding-intake SKILL.md cites template line ranges for the comment blocks it strips (finding template "lines 1–22"; journal template "lines 1–18" and "lines 29–84") that are stale relative to the current templates (the comment blocks actually occupy lines 1–26 and lines 1–24 / 35–90 respectively). The skill's *spirit* (strip the leading HTML comment block; strip the closing commented-out skeleton block) is unambiguous and was followed. Whether to track this as its own finding is a triage-time question and is **not** the substance of this finding.
- Route subtype unknown at intake. Triage will likely consider whether remediation belongs at `/spec-write` (closeout commit step, or OUTPUT-FORMAT reminder), at `/spec-execute` (formalize the orient-phase detection as the intentional safety net), or both.
