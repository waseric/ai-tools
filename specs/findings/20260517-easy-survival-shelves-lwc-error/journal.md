# LWC "Missing API" error on shelves in Easy Survival — Journal

## 2026-05-17 — Intake: LWC plugin "Missing API" error on shelf interaction in Easy Survival; absent on Normal

**Captured by:** waseric; persona-frame: intake
**Signal source:** text + URL (forum thread URL plus operator-supplied PDF snapshot of the thread page)
**New status:** `intake`
**Notes:**
- Source thread is on the Sandlot Minecraft forum, auth-walled: https://www.sandlotminecraft.com/threads/error-when-placing-items-in-shelves.39846/. Operator pre-fetched the page as a PDF and supplied it at session intake, so the load-bearing content (the error text, the cross-world test "Tested Normal and it was fine," and the second reporter's confirmation) is captured verbatim in `finding.md`'s `External references` field rather than relying on the live URL remaining reachable for unauthenticated parties.
- No live URL re-fetch was attempted by the agent. Rationale per SKILL.md OP #3 (never silently swallow a fetch outcome — choosing not to attempt is still an outcome to surface): the forum is auth-walled and would have returned a login redirect, and the operator-supplied PDF already provides the durable snapshot the policy is meant to secure. This deviation from "attempt a fetch if capable" is itself the right call when the snapshot already exists; recorded here so reviewers can revisit at RC-3a if they disagree.
- The two-reporter pattern (GargoyleAnt initial + NeonLights10927 confirmation) is recorded in the `Reported by` field per the T-05 inline-list convention. The single-field schema constraint on `Reported by` was already surfaced as a friction observation in the T-05 finding's journal; not re-raising here.
- This finding is the T-03 real-signal dogfood for the finding-intake skill (spec: [specs/20260517-finding-intake-skill/feature.md](../../20260517-finding-intake-skill/feature.md)). It joins the T-05 `tab-display-issues` finding as the second operational-domain living example in `specs/findings/`, alongside the T-02 synthetic `test-only-signal-synthetic-fixture` retained as a methodology-domain regression reference.

## 2026-05-17 — Triaged: LWC `:Missing API` on shelves — operational, reliably reproducible, Easy-Survival-isolated; advisory + P4

**Triaged by:** waseric; Sandlot administrator; persona-frame: triage
**Prior status:** `intake`
**New status:** `triaged`
**Reproducibility outcome:** reliably — two distinct reporters (GargoyleAnt 2026-05-10, NeonLights10927 2026-05-16) produce the same verbatim error across two interaction modes (place/remove, right-click); the in-thread cross-world control (Normal Survival) negative result strengthens the repro signal by isolating the failure to Easy Survival.
**Domain/severity changes from intake:** Domain unchanged (operational confirmed). Severity set to `advisory` — was `<placeholder>` at intake (severity is a triage-time field per the schema), so this is a first-population, not a delta from a prior triage value. Operational urgency set to `P4` — first-population for the same reason. The `advisory`/`P4` pairing reflects that shelves are not core to gameplay and a player workaround exists (avoid shelf interactions on Easy Survival until investigated); the signal is real and worth investigating but is not blocking active play.
**Skip-investigation decision (if any):** end at `triaged`. The skip-investigation surface was deliberately not exercised in T-03 (operator decision at execution time, recorded in [specs/20260517-finding-triage-skill/journal.md](../../20260517-finding-triage-skill/journal.md) T-03 entry). The natural route, when this finding is later triaged onward, is likely `spec-write` against a future ArenaCTF or BattleTracker spec — or `defer` with a watch condition pending plugin-author engagement — but the route decision belongs to a later transition (investigation, or a future re-route after investigation), not to this Triaged entry. Consequence: the two-journal-entry skip-route discipline (per [SKILL.md WHAT NOT TO DO bullet 6](../../../.agents/skills/finding-triage/SKILL.md)) remains verified only by inspection across T-02 and T-03; RC-3b reviewer should be aware. See the T-03 journal entry in the feature-spec journal for the operator's reasoning.
**Pointer revalidation:** forum-thread URL https://www.sandlotminecraft.com/threads/error-when-placing-items-in-shelves.39846/ — `treated-as-static`. Intake Summary judged rich (≥3 sentences, two named reporters, verbatim error text quoted, operator-supplied PDF snapshot supplies the durable evidence per intake journal note). Soft default per [SKILL.md Phase 2 pointer-revalidation policy](../../../.agents/skills/finding-triage/SKILL.md); operator accepted without override. No live re-fetch attempted (the forum is auth-walled per intake journal; the PDF snapshot is the authoritative artifact).
**Notes:**
- **Persona-frame override exercised on the first real-signal dogfood.** Suggested frame from the Domain → frame derivation was `business analyst`; operator overrode to `Sandlot administrator` because the operator IS the Sandlot administrator and that frame fits the work better. This is the first exercise of the override path codified in [feature spec §5.3](../../20260517-finding-triage-skill/feature.md#53-persona-frame-derivation-resolves-design-spec-oq-3) and resolves the watch item from spec §10 Risks ("Persona-frame override surface is rarely used, defaulting back to a single suggestion the operator accepts blindly") in the direction of *used*. RC-3b reviewer evidence.
- **Hard-facts discipline held.** No cause hypothesis appears in the Triage section. The intake-time hypothesis (":Missing API → plugin API surface missing/version-mismatched") is recorded in the finding's Triage notes field as *deferred to investigation*, not as a Triage claim, per [SKILL.md OPERATING PRINCIPLE #1](../../../.agents/skills/finding-triage/SKILL.md) and the WHAT NOT TO DO bullet on causal hypotheses.
- **Triage mode.** Produced in structured-input mode by Claude on the operator's behalf via `/spec-execute`, with the load-bearing operator-judgment fields (persona-frame override, severity + urgency, skip-investigation decision, pointer-revalidation choice) collected up-front via a single `AskUserQuestion` round-trip rather than the skill's per-field interactive prompts. The 60-second-vs-not honest comparison against the intake NFR (per feature-spec §7 T-03 mode framing) is therefore *not* directly observed here; the spec accepts this trade — interactive prompts are an option the skill supports, not a discipline the skill enforces. Surfaced for RC-3b reviewer awareness.
- **No further reporters surfaced between intake and triage** (same calendar day). Reporter list is stable; the Reported-by inline-list captured at intake remains current.
